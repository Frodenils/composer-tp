<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Logiciel - {{ title }}</title>
</head>
<body>
    <h1>Logiciel - {{ title }}</h1>
    <p>Voici une page HTML, complétée par le texte : {{ text }}</p>
    <ul>
        {% for user in users %}
        <li>{{ user.email }}</li>
        {% endfor %}
    </ul>
</body>
</html>